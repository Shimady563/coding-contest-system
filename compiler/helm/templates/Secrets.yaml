apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
  labels:
    {{ include  }}
data:
  spring.datasource.url: {{ .Values.global.db.url | b64enc }}
  spring.datasource.password: {{ .Values.global.db.password | b64enc }}
  spring.datasource.username: {{ .Values.global.db.username | b64enc }}
