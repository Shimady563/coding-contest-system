apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}
  labels:
    {{- include "auth.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.auth.replicas }}
  selector:
    matchLabels:
      {{- include "auth.selectorLabels" . | nindent 6 }}
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        {{- include "auth.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          serviceAccountName: {{ .Values.global.serviceAccount }}
          image: {{ .Values.global.dockerRepositoryName }}/{{ .Values.auth.dockerImageName }}:{{ .Values.global.dockerTag }}
          ports:
            - name: Auth
              protocol: TCP
              containerPort: {{ .Values.auth.server.internalPort }}
          readinessProbe:
            httpGet:
              path: {{ .Values.auth.server.basePath }}/actuator/health/readiness
              port: {{ .Values.auth.server.internalPort }}
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: {{ .Values.auth.server.basePath }}/actuator/health/liveness
              port: {{ .Values.auth.server.internalPort }}
            initialDelaySeconds: 60
            periodSeconds: 20
            failureThreshold: 10
          resources:
            {{- toYaml .Values.auth.resources | nindent 12 }}
          volumeMounts:
            - name: {{ .Chart.Name }}-config
              mountPath: {{ .Values.global.mountPath.config }}
            - name: {{ .Chart.Name }}-secret
              mountPath: {{ .Values.global.mountPath.secret }}
      volumes:
        - name: {{ .Chart.Name }}-config
          configMap:
            name: {{ .Chart.Name }}-config
        - name: {{ .Chart.Name }}-secret
          secret:
            secretName: {{ .Chart.Name }}-secret